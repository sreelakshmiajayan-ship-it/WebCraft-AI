# ── Frontend Environment Variables (EXAMPLE — safe to commit) ─
# Copy this file to .env and fill in your values.
# NEVER commit the real .env file.

# Backend API base URL
# Local development  → http://localhost:5000
# Production (Render) → https://YOUR_RENDER_APP.onrender.com
VITE_API_URL=https://webcraft-ai-98c2.onrender.com